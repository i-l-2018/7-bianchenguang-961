git分为远程仓库（remote）本地仓库（repository）工作区（work piace）暂存区（index stage）。
在本地文件有改动后，右键git GUI后出现在unstaged changes然后点击到暂存区，commit（提交）到本地仓库，还需要添加merge commit message（就是说明），然后再push到远程仓库。
当gitub上有改动时，从remote上fetch（取来，拿来），然后merge合并。
